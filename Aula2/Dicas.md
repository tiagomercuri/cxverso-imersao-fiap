1- instalar o complicador
npm install -g typescript

2- Versão:
tsc -v

tsc --init

// Closures
// acontece quando uma função lembra do escopo que foi criada mesmo depois de ser executada

Atividade
Criar um projeto de uma loja de doces gourmet (usando todas as tecnicas usadas no projeto da loja gamer) estilizado